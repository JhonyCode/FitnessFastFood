{% extends 'base.html.twig' %}

{% block title %}Publicaciones{% endblock %}

{% block body %}
    <h1>Publicaciones</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ publicacione.id }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ publicacione.estado }}</td>
            </tr>
            <tr>
                <th>Resumen</th>
                <td>{{ publicacione.resumen }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ publicacione.slug }}</td>
            </tr>
            <tr>
                <th>Titulo</th>
                <td>{{ publicacione.titulo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_publicaciones_index') }}">back to list</a>

    <a href="{{ path('app_admin_publicaciones_edit', {'id': publicacione.id}) }}">edit</a>

    {{ include('admin/publicaciones/_delete_form.html.twig') }}
{% endblock %}
