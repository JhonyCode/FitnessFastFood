<form method="post" action="{{ path('app_admin_valoraciones_delete', {'id': valoracione.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ valoracione.id) }}">
    <button class="btn">Delete</button>
</form>
